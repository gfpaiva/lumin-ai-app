## 1. Modificação da Interface (UI)

- [x] 1.1 Atualizar a renderização de ações das atividades em `LessonDetailFeature.tsx` para verificar `activity.completed` durante o modo de edição (`isEditing === true`).
- [x] 1.2 Renderizar uma tag/badge estática de "Concluído" (não clicável) quando a atividade estiver concluída no modo de edição.

## 2. Validação e Testes

- [x] 2.1 Verificar manualmente ou via testes a exibição da tela no modo normal e no modo de edição com atividades concluídas e não concluídas.
